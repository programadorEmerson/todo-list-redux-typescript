import { styled } from '@mui/material';
import { GridToolbarContainer } from '@mui/x-data-grid';

const CustomGridToolbarContainer = styled(GridToolbarContainer)`
  display: flex;
  justify-content: flex-end;
  align-items: center;
`;

export { CustomGridToolbarContainer };
